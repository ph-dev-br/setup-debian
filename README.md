# Setup Debian
Scripts for set my setup in Debian

## Quick Setup

Run commands below for full setup:
```bash
git clone --depth=1 'https://github.com/ph-dev-br/setup-debian' /tmp/setup-debian
cd /tmp/setup-debian
bash setup.sh
```

